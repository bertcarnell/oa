#ifndef PRIMES_H
#define PRIMES_H

#include <cmath>
#include <cstdio>

namespace oa {
	int isprime(int p );
	void ispcheck();
	void primepow(int q, int* p, int* n, int* isit );
	int isprimepow(int q );
	int ipow( int a, int b );
	void fqpncheck();
}

#endif
